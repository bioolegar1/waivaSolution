export interface ICapabilityBlock {
    title: string;
    description: string;
    imageAlt: string;
    // Na prática você usará URLs de imagens reais,
    // aqui usarei cores para simular os placeholders da imagem
    imageGradient: string;
}